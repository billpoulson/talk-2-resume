{
  "name": "@talk2resume/client",
  "version": "1.0.0",
  "scripts": {
    "dotenv-pull": "npx dotenv-vault@latest pull",
    "dotenv-push": "npx dotenv-vault@latest push",
    "dotenv:build": "npx dotenv-vault@latest build",
    "generate-env:watch": "nodemon --watch .env --exec tsx generate-env.ts",
    "generate-env": "tsx generate-env.ts",
    "docker-build": "docker build ."
  },
  "dependencies": {
    "@types/multer": "^1.4.12",
    "@types/three": "^0.172.0",
    "multer": "^1.4.5-lts.1",
    "three": "^0.172.0"
  }
}
